import type { RankManifestEntry } from './types';

// Generated values are replaced by scripts/generate-rank-manifest.mjs during build.
export const CURRENT_RANK_MANIFEST: Record<string, RankManifestEntry> = Object.freeze({
  cl100k_base: Object.freeze({ id: 'cl100k_base', version: 'uninitialized', model: 'cl100k_base' }),
  o200k_base: Object.freeze({ id: 'o200k_base', version: 'uninitialized', model: 'o200k_base' }),
});

export const CURRENT_RANK_CACHE_VERSION = 'uninitialized';
